import { LazyLoadRowDirective } from './lazy-load-row.directive';

describe('LazyLoadRowDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadRowDirective();
    expect(directive).toBeTruthy();
  });
});
