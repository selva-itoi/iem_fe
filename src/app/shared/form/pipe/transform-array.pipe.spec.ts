import { TransformArrayPipe } from './transform-array.pipe';

describe('TransformArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
