import { FormControlActionPipe } from './form-control-action.pipe';

describe('FormControlActionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlActionPipe();
    expect(pipe).toBeTruthy();
  });
});
